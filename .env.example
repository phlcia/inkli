# Supabase (required)
EXPO_PUBLIC_SUPABASE_URL=your_supabase_project_url
EXPO_PUBLIC_SUPABASE_ANON_KEY=your_supabase_anon_key

# Google Books (optional – app works without it but with lower rate limits)
EXPO_PUBLIC_GOOGLE_BOOKS_API_KEY=your_google_books_api_key_here

# Ask feature: Grok API key is NOT set here. Set it as a Supabase secret:
#   supabase secrets set GROK_API_KEY=your_xai_api_key
# Optional: custom system prompt (otherwise a default is used):
#   supabase secrets set GROK_SYSTEM_PROMPT="Your custom prompt..."
